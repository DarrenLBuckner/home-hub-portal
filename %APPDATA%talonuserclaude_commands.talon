tag: user.claude_code
-

# Claude Code Voice Commands
# Voice commands for interacting with Claude Code efficiently

# Basic Navigation
claude home: key(ctrl-shift-p)
claude terminal: key(ctrl-`)
claude explorer: key(ctrl-shift-e)
claude search: key(ctrl-shift-f)

# File Operations  
claude open file: key(ctrl-o)
claude new file: key(ctrl-n)
claude save: key(ctrl-s)
claude save all: key(ctrl-k s)

# Code Navigation
claude go to line: key(ctrl-g)
claude go to symbol: key(ctrl-shift-o)
claude find definition: key(f12)
claude find references: key(shift-f12)
claude back: key(alt-left)
claude forward: key(alt-right)

# Editing Commands
claude comment: key(ctrl-/)
claude duplicate line: key(shift-alt-down)
claude delete line: key(ctrl-shift-k)
claude move line up: key(alt-up)
claude move line down: key(alt-down)

# Claude Code Specific
claude run: "Run this command"
claude test: "Test the current functionality"
claude debug: "Debug this issue"
claude explain: "Explain this code"
claude fix: "Fix this error"
claude optimize: "Optimize this code"
claude refactor: "Refactor this function"

# Git Commands
claude status: "Check git status"
claude commit: "Create a git commit"
claude push: "Push changes to repository"
claude pull: "Pull latest changes"

# Development Tasks
claude build: "Build the project"
claude start server: "Start the development server"
claude install: "Install dependencies"
claude lint: "Run linting"
claude format: "Format the code"

# Project Management
claude todo: "Add this to todo list"
claude priority: "Mark this as high priority"
claude complete: "Mark this task as complete"
claude review: "Review this implementation"

# Documentation
claude document: "Add documentation for this"
claude readme: "Update the README file"
claude comment this: "Add comments to explain this code"

# Deployment
claude deploy: "Deploy to production"
claude test deploy: "Deploy to staging"
claude rollback: "Rollback deployment"

# Database
claude migrate: "Run database migrations"
claude seed: "Seed the database"
claude backup: "Create database backup"

# Environment
claude env: "Check environment variables"
claude config: "Update configuration"
claude logs: "Show application logs"

# Quick Actions
claude quick fix: key(ctrl-.)
claude select all: key(ctrl-a)
claude copy: key(ctrl-c)
claude paste: key(ctrl-v)
claude undo: key(ctrl-z)
claude redo: key(ctrl-y)

# Multi-cursor
claude add cursor: key(ctrl-alt-down)
claude select next: key(ctrl-d)
claude select all occurrences: key(ctrl-shift-l)

# Panel Controls
claude toggle sidebar: key(ctrl-b)
claude toggle terminal: key(ctrl-`)
claude toggle problems: key(ctrl-shift-m)
claude toggle output: key(ctrl-shift-u)

# Zoom
claude zoom in: key(ctrl-=)
claude zoom out: key(ctrl--)
claude reset zoom: key(ctrl-0)